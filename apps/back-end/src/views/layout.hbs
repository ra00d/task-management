<!DOCTYPE html>
<html lang='{{lang}}'>

  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <link rel='stylesheet' href='css/app.css' />
    <title>{{title}}</title>
  </head>

  <body>
    <div class='drawer'>
      <input id='app-drawer' type='checkbox' class='drawer-toggle' />
      <div class='drawer-content flex flex-col relative'>
        <div class='w-full navbar bg-base-300 sticky top-0 z-50 bg-opacity-95'>
          <div class='flex-none lg:hidden'>
            <label
              htmlFor='app-drawer'
              aria-label='open sidebar'
              class='btn btn-square btn-ghost'
            >
              <svg
                xmlns='http://www.w3.org/2000/svg'
                fill='none'
                viewBox='0 0 24 24'
                class='inline-block w-6 h-6 stroke-current'
              >
                <path
                  strokeLinecap='round'
                  strokeLinejoin='round'
                  strokeWidth='2'
                  d='M4 6h16M4 12h16M4 18h16'
                ></path>
              </svg>
            </label>
          </div>
          <h1 class='flex-1 px-2 mx-2 uppercase'>{{name}}</h1>
          <div class='flex-none hidden lg:flex sticky top-0  gap-2'>
            <ul class='menu menu-horizontal items-center'>
              {{#each navItems}}

                <li><a href='{{this.path}}'>{{t this.name}}</a></li>
              {{/each}}

              <li><a href='/control/login' class='btn btn-primary btn-sm'>{{t
                    'nav.login'
                  }}</a></li>
            </ul>
            <div class="flex gap-2 "> 
              {{> tools/themeSwitcher}}
              {{> tools/langSwitcher}}
            </div> </div>
        </div>
        {{{body}}}
      </div>
      <div class='drawer-side'>
        <label
          htmlFor='my-drawer-3'
          aria-label='close sidebar'
          class='drawer-overlay'
        ></label>
        <ul class='menu p-4 w-80 min-h-full bg-base-200'>
          {{#each navItems}}

            <li><a>{{this.name}}</a></li>
          {{/each}}
        </ul>
      </div>
    </div>


    <script type="module" defer  nonce='{{cspNonce}}' src="js/script.js"></script> 
  </body>

</html>
